services:
  db:
    image: mysql:8.0
    restart: always
    environment:
      MYSQL_DATABASE: ${DB_NAME}
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD}
    volumes:
      - mysql_data:/var/lib/mysql
    networks:
      - internal
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "--password=${DB_PASSWORD}"]
      interval: 10s
      timeout: 5s
      retries: 10

  app:
    build: .
    restart: always
    depends_on:
      db:
        condition: service_healthy
    env_file: .env
    volumes:
      - app_images:/app/public/images
      - app_storage:/app/storage/logs
    networks:
      - internal
      - web
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=web"
      - "traefik.http.routers.idoil.rule=Host(`${DOMAIN}`)"
      - "traefik.http.routers.idoil.entrypoints=websecure"
      - "traefik.http.routers.idoil.tls.certresolver=letsencrypt"
      - "traefik.http.services.idoil.loadbalancer.server.port=80"

volumes:
  mysql_data:
  app_images:
  app_storage:

networks:
  internal:
  web:
    external: true
